# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com

# Ollama Configuration (fallback if local model not available)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:9b

# Local GGUF Model (takes priority over Ollama if file exists)
LOCAL_MODEL_PATH=/home/pranavvv/models/qwen3.5-9b/qwen_3.5_9B_Q4_K_M.gguf

# Database
DB_PATH=./data/leads.db

# WhatsApp Web persistent session directory
WHATSAPP_USER_DATA_DIR=./whatsapp_session

# Dashboard
DASHBOARD_PORT=8080
